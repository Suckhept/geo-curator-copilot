packages:
  - 'packages/*'
# pnpm 11 build-script approval: replaces onlyBuiltDependencies (v11 ignores it
# and scaffolds this key itself on install — verified with 11.5.3 fresh clone)
allowBuilds:
  esbuild: true
verifyDepsBeforeRun: false
